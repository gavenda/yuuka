{
	/* The browser half. The server half (functions/ and test/) is a standalone
	   project — see tsconfig.functions.json — because it targets the Workers
	   runtime rather than the DOM. */
	"files": [],
	"references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }]
}
